export interface RpsResult {
  result: number;
  computerChoice: number;
  gamesPlayed: number;
  userWins: number;
  userLosses: number;
  draws: number;
  streak: number;
  // rock: number;
  // paper: number;
  // scissors: number;
  // rockWins: number;
  // paperWins: number;
  // scissorsWins: number;
}
